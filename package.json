{
  "name": "vain-pilot",
  "version": "1.0.0",
  "description": "- 1.1. Go to the directory/folder named \"loadVainDBfromTSV\". - 1.2. run npm install. - 1.3. Change userid and password to your specific DB credentials. - 1.4. run the following command 'node vaininstall.js' .",
  "main": "index.js",
  "scripts": {
    "start": "(cd init-database && npm install && npm start) && ((cd vain-backend && npm install && npm test) & (cd vain-client && npm install && npm run dev))",
    "test": "(cd init-database && bun install && bun start) && ((cd vain-backend && bun install && bun test) & (cd vain-client && bun install && bun run dev))"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pikachungg/vain-pilot.git"
  },
  "author": "Daniel Chung",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pikachungg/vain-pilot/issues"
  },
  "homepage": "https://github.com/pikachungg/vain-pilot#readme",
  "devDependencies": {
    "autoprefixer": "^10.4.13"
  },
  "dependencies": {
    "react-icons": "^4.7.1"
  }
}
